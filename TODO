global bug and to do list

FIXME:
messages don't stay in view when they first hit the bottom of the screen
text is too heavy in Firefox

TODO:
actually write some plot
figure out what the initial scoop is
create favicon
let users horizontally scroll the prompts using their vertical scroll wheels
move typing indicator to below textfield
if prompt is wider than the screen, scroll along the screen as the user types it out
